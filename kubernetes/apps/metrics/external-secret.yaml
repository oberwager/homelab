apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: grafana-admin
  namespace: monitoring
spec:
  target:
    name: grafana-admin
  data:
    - secretKey: password
      remoteRef:
        key: grafana-admin
        property: password
